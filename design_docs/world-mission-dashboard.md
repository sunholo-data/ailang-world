# Mission Dashboard — Ailang World

*Snapshot, overwritten every iteration. History: charter STATUS + `world-mission-log.md`.
Written: iteration 116, 2026-08-24.*

## Where we are

- **dev**: GREEN at `3a51f16`; latest landed milestone remains `WB.E`.
- **In flight**: item 14 `w-workbench-read-only`, milestone `WB.F`.
- **Local verified commit**: `a96fd67` (+90 test lines); not pushed, evaluated, merged, or landed.
- **Blocker**: Sonnet weekly quota, rc=1 at 04:09 CEST; resets Mon 07:00 CEST.
- **Resume predicate**: after 07:00, re-probe Sonnet and independently evaluate `a96fd67`.
- **NEXT**: resume `WB.F`; do not start `WB.G`.

## Routing · cost · parked

- Executor `codex:gpt-5.6-sol` completed; evaluator `sonnet` did not run.
- Generator ≠ judge preserved. Metered spend **$0.00**.
- **Nothing is blocked on a human answer** (`scripts/mission_decisions.sh --open` empty).
- Row 5 remains blocked upstream on `ailang#764` (OPEN, no maintainer reply).

## Bookkeeping

- Live issue #68; 38 comments.
- Weekly rotation becomes due at Mon 2026-08-24 07:00 CEST.
